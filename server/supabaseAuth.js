export async function requireUser(req,res,next){const auth=req.headers.authorization||"";if(!auth.startsWith("Bearer "))return res.status(401).json({error:"Authentication required"});const token=auth.slice(7);const base=process.env.SUPABASE_URL,anon=process.env.SUPABASE_ANON_KEY;if(!base||!anon)return res.status(500).json({error:"Supabase server configuration missing"});try{const r=await fetch(base+"/auth/v1/user",{headers:{Authorization:"Bearer "+token,apikey:anon}});const user=await r.json();if(!r.ok)throw new Error(user.message||"Invalid session");req.user=user;next()}catch(e){res.status(401).json({error:e.message})}}